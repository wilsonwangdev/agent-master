{
  "name": "agent-master",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build/build.mjs",
    "clean": "rm -rf site",
    "setup": "git config core.hooksPath .githooks"
  },
  "dependencies": {
    "marked": "^15.0.0"
  }
}
